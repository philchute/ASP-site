@page
@model ASP_site.Pages.FightingGames.IndexModel

@{
    ViewData["Title"] = "Fighting Games";
}

<div class="container">
    <div class="row">
        <div class="col">
            <div class="markdown-content" style="max-width: 800px; margin: 0 auto;">
                @Html.Raw(Model.MarkdownHtml)
            </div>
        </div>
    </div>
</div>